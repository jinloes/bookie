{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Bookie",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-message"
  ]
}
