/**
 * Composition adapters for retained HTTP and persistence compatibility. Only other compatibility
 * adapters may depend on this package; normal capability code may not. Adapters here may depend on
 * multiple application ports while legacy tables and endpoints remain available for rollback.
 */
package com.bookie.compatibility;
